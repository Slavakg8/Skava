const Discord = require(‘discord.js’);

const {

prefix,! 

token,ODYwODY5NTkyMTMzNDAyNjc0.YOBhSA.-_DpeWbXN7zOrjOe3_fqWJrHMu0 

} = require(‘./config.json’);

const ytdl = require(‘ytdl-core’);
const client =(Slava[Bot]);

client.login(token);
client.once(‘ready’, () => {

    console.log(‘Ready!’);
    
    });
    
    client.once(‘reconnecting’, () => {
    
    console.log(‘Reconnecting!’);
    
    });
    
    client.once(‘disconnect’, () => {
    
    console.log(‘Disconnect!’);
    
    });